<%= form_with(model: aircraft, local: true) do |form| %>
  <% if aircraft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aircraft.errors.count, "error") %> prohibited this aircraft from being saved:</h2>

      <ul>
      <% aircraft.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :aircraft_name %>
  </div>

  <div class="field">
    <%= form.label :drone_type %>
    <%= form.text_field :drone_type, id: :aircraft_drone_type %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :aircraft_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
