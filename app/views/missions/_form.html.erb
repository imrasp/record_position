<%= form_with(model: mission, local: true) do |form| %>
  <% if mission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mission.errors.count, "error") %> prohibited this mission from being saved:</h2>

      <ul>
      <% mission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :aircraft_id %>
    <%= form.text_field :aircraft_id, id: :mission_aircraft_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :mission_name %>
  </div>

  <div class="field">
    <%= form.label :starttime %>
    <%= form.time_select :starttime, id: :mission_starttime %>
  </div>

  <div class="field">
    <%= form.label :endtime %>
    <%= form.time_select :endtime, id: :mission_endtime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
